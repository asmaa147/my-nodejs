<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Articles Page</title>
  </head>
  <body>
    <h1>Welcome <%= name %>!, << Articles Page >></h1>

    <div style="background-color: orange; padding: 0 1rem; border-radius: 10px;">
      <% for( let art of articles) { %>
      <div style="display: inline-flex; justify-items: center; flex-direction: column; width: 100%; border: 2px solid #fff; border-radius: 10px; padding: 0.5rem 1rem; margin: 2rem;">
        <h1> <%= art.title?.toUpperCase() %> </h1>
        <p> <%= art.body %> </p>
      </div>
      <% } %>
    </div>
  </body>
</html>
